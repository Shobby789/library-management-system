import React from "react";
import BorrowRequestList from "../../components/BorrowRequests/BorrowRequestList";

const BorrowRequest = () => {
  return <BorrowRequestList />;
};

export default BorrowRequest;
